/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "graqhql-server-app.onrender.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
